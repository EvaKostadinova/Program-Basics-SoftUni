function expenses(dogsFood, catsFood){
    let dogsFoodPrice = 2.5;
    let catsFoodPrice = 4;
    let allExpenses = (dogsFood * dogsFoodPrice) + (catsFood * catsFoodPrice);
    console.log(allExpenses + " lv.")
}

expenses(13, 9);